// //week 3 
// # Statements  and Blocks
// - ใช้ {
//       // สร้าง Block 
//       // ในBlock มี declarations and statementsโดย ปิด ด้วย;
// }

// # if - else;
// รูปแบบ if (expression)
//          statement1
//       else 
//         statement2

//     ex; if (a==0){
//            x+=1; // 1block
//     }
//         else{
//            x = 0; 
//     }

// # Switch // เป็นการตรวจสอบเงื่อนไข โดยดูความmatchกัน แล้วเอามาทำงาน 
// รูปแบบ 
// switch (expression){
//     case const- expr:
//       statements
//     default:// มีหรือไม่ก็ได้ ถ้ามีจะใช้เมื่อ ค่าexpression ไม่เข้ากับเคสไหนเลย 
//       statements
// }

// #Loops - While and for
// 1)รูปแบบ while 
// while (exp){ // ถ้า exp เป็นจริง จะทำงานจนกว่าจะเป็นเท็จ 
//    statement
//    // while (1) คือ while True จะหยุดเมื่อใส่ เงื่อนไข break; ลงไป
// }

// 2) รูปแบบ for 
// for (ค่าที่เราตั้ง ; เงื่อนไขการวนลูป ;การเปลี่ยนแปลงภายในลูป){//ถ้าไม่ตรงเงื่อนไขในการวนลูป for loopจะหยุด
//     statement
// }

// ถ้าเขียน for loop แบบ 
// for(;;){// การใส่แบบนี้จะเป็นการทำให้ลูปไม่รู้จบ (infinite loop)
//     printf("..."); // ปริ้นไปเรื่อยๆ เพราะไม่มีตัวกำหนดหยุด
// }

// # Loops - Do - While
// //สิ่งที่ต่างจาก while and for คือเงื่อนไขจะอยู่ด้านล่าง
// รูปแบบ 
// do 
//   statement// เริ่มทำงานก่อน 
// while (exp); // ดูว่าเป็นจริงหรือเท็จ ถ้าจริงจะวนทำเรื่อยๆ

// #Break and  Con
// - สามารถนำไปใส่ในเงื่อนไขเพื่อควบคุมการทำงานของโปรแกรม เพื่อให้หยุด หรือ ทำต่อ 
// โดย //*Break*// ทำให้โปรแกรมหลุดจากloop
// //*continue*// ทำให้ลูปวนซำ้ โดย while and Do while จะทำให้ข้ามไปดูเงื่อนไขเลย 






